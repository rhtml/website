
import { createUseStyles } from 'react-jss';
import colors from '../../css/colors';

export default createUseStyles({
  red: {
    color: colors.red,
  },
  white: {
    color: colors.white,
  },
});
